@PACKAGE_INIT@

if (NOT TARGET Alloy::Core)
  include("${CMAKE_CURRENT_LIST_DIR}/AlloyCoreTargets.cmake")
endif ()
if (NOT TARGET Alloy::Alloy)
  include("${CMAKE_CURRENT_LIST_DIR}/AlloyTargets.cmake")
endif ()